# Rules

Path-scoped rules loaded automatically when Claude works in matching files or directories.

- **Trigger:** Automatic (path matching)
- **Scope:** Specific paths via frontmatter `files: ["src/frontend/**", "components/**"]`
- **Use for:** Conventions by area (frontend, backend, tests, docs)

Split large CLAUDE.md content here to keep the main file under ~100 lines.
