# Skills

Auto-invoked capabilities when Claude determines they’re needed (e.g. parse PDF, run a linter).

- **Trigger:** Automatic (model judgment)
- **Scope:** Reusable across projects
- **Structure:** One folder per skill with required `SKILL.md` (name, description, usage conditions, steps)

Use for capabilities that should run without the user typing a slash command.
