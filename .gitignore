# Dependencies
node_modules/
.pnp/
.pnp.js

# Next.js
.next/
out/
build/

# Turbopack
.turbo/

# Vercel
.vercel/

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Pipeline: raw LinkedIn exports (may contain PII, unparsed columns beyond our schema)
data/sources/linkedin/*.csv
data/sources/linkedin/*.zip
data/sources/linkedin/*.zip:Zone.Identifier

# Pipeline: binary build artifacts (regenerable via npm run export)
# Covers recruiter-friendly naming (Paul-Prae-Resume.pdf) and fallback (Resume.pdf)
data/generated/*-Resume.pdf
data/generated/*-Resume.docx
data/generated/Resume.pdf
data/generated/Resume.docx

# Pipeline: version archive (timestamped copies of each export, regenerable)
data/generated/versions/

# Pipeline files that ARE committed:
#   data/sources/knowledge/**/*.json — curated career knowledge (recruiter-facing, used by Phase 2 RAG)
#   data/generated/career-data.json — canonical career data model (portable across machines)
#   data/generated/<Name>-Resume.md — AI-generated resume (consumed by Next.js build + Vercel deploy)
#   data/generated/VERSIONS.md     — version history manifest (data lineage + recruiter tracking)

# Machine-specific inventory (hostnames, usernames, build numbers)
machine-inventory.local.md
*.local.md
!.env.local.example

# OS files
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~

# IDE / Editor
.vscode/
.idea/
.cursor/
*.code-workspace

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Testing
coverage/
.nyc_output/

# Misc
*.pem
